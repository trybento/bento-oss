import { WorkingState } from '../types';

/**
 * Receives response from the NpsSurveys query and updates the state accordingly.
 */
export default function npsSurveysHydrated(_state: WorkingState) {}
